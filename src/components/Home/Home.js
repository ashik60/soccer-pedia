import React from 'react';
import Teams from '../Teams/Teams';

const Home = () => {
    return (
        <div className="container">
            <Teams />
        </div>
    );
};

export default Home;